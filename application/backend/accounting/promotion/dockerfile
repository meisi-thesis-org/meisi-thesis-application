FROM node:18

RUN apt-get update && apt-get install -y --no-install-recommends apt-utils

WORKDIR /usr/src/app

COPY application/backend/accounting/promotion/package.json application/backend/accounting/promotion/yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

RUN cd application/backend/accounting/promotion && yarn build

EXPOSE 8008

CMD ["yarn","start"]