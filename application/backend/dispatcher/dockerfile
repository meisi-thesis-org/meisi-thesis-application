FROM node:18

RUN apt-get update && apt-get install -y --no-install-recommends apt-utils

WORKDIR /usr/src/app

COPY application/backend/dispatcher/package.json application/backend/dispatcher/yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

RUN cd application/backend/dispatcher && yarn build

EXPOSE 8000

CMD ["yarn","start"]